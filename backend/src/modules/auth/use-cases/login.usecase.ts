export const LoginUseCase = async (firebaseToken: string) => {
  // verify firebase token
  // find user
  // create user if user doesn't exist
  // create wallet
  // create ledger account
  // generate jwt
  // return response
};
